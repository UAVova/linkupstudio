<%= form_for ([@task, @task.comments.new]) do |f| %>
	<div class="form-group">
	  <%= f.text_area :content, rows: 5, class: "controls" %>
	</div>
	<div class="form-group">
	  <%= f.submit "Submit comment", class: "btn btn-success" %>
	</div>
<% end  %>